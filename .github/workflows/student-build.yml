name: Student Android APK Build

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'apps/student/**'
      - '.github/workflows/student-build.yml'

permissions:
  contents: read

env:
  CI: 1
  NODE_ENV: production

jobs:
  build-student:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: apps/student/package.json

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Install Student dependencies
        working-directory: apps/student
        run: npm install --no-audit --no-fund

      - name: Verify Student Expo dependency tree
        working-directory: apps/student
        run: |
          node -e "const p=require('./package.json'); console.log('Expo:',p.dependencies.expo); console.log('expo-router:',p.dependencies['expo-router']); console.log('React Native:',p.dependencies['react-native'])"
          npx expo-doctor --verbose

      - name: Generate Student Android project
        working-directory: apps/student
        env:
          CI: 1
          NODE_ENV: production
        run: npx expo prebuild --platform android --clean

      - name: Repair and verify Student Expo autolinking
        working-directory: apps/student
        run: |
          set -e
          SETTINGS="android/settings.gradle"
          if [ ! -f "$SETTINGS" ]; then
            echo "ERROR: Expo prebuild did not generate android/settings.gradle"
            exit 1
          fi

          if ! grep -q 'expoAutolinking.useExpoModules' "$SETTINGS"; then
            echo "Expo autolinking block missing after prebuild; restoring SDK 53-compatible settings.gradle."
            cat > "$SETTINGS" <<'EOF'
pluginManagement {
  def reactNativeGradlePlugin = new File(
    providers.exec {
      workingDir(rootDir)
      commandLine("node", "--print", "require.resolve('@react-native/gradle-plugin/package.json', { paths: [require.resolve('react-native/package.json')] })")
    }.standardOutput.asText.get().trim()
  ).getParentFile().absolutePath
  includeBuild(reactNativeGradlePlugin)

  def expoPluginsPath = new File(
    providers.exec {
      workingDir(rootDir)
      commandLine("node", "--print", "require.resolve('expo-modules-autolinking/package.json', { paths: [require.resolve('expo/package.json')] })")
    }.standardOutput.asText.get().trim(),
    "../android/expo-gradle-plugin"
  ).absolutePath
  includeBuild(expoPluginsPath)
}

plugins {
  id("com.facebook.react.settings")
  id("expo-autolinking-settings")
}

extensions.configure(com.facebook.react.ReactSettingsExtension) { ex ->
  if (System.getenv('EXPO_USE_COMMUNITY_AUTOLINKING') == '1') {
    ex.autolinkLibrariesFromCommand()
  } else {
    ex.autolinkLibrariesFromCommand(expoAutolinking.rnConfigCommand)
  }
}

expoAutolinking.useExpoModules()

rootProject.name = 'studybro'
expoAutolinking.useExpoVersionCatalog()
include ':app'
includeBuild(expoAutolinking.reactNativeGradlePlugin)
EOF
          fi

          grep -q 'expoAutolinking.useExpoModules' "$SETTINGS"
          grep -q 'expo-autolinking-settings' "$SETTINGS"

          if [ ! -f android/build.gradle ]; then
            echo "ERROR: android/build.gradle missing"
            exit 1
          fi
          if ! grep -q 'expo-root-project' android/build.gradle; then
            echo 'apply plugin: "expo-root-project"' | cat - android/build.gradle > android/build.gradle.tmp
            mv android/build.gradle.tmp android/build.gradle
          fi

          if ! grep -q 'autolinkLibrariesWithApp()' android/app/build.gradle; then
            echo "ERROR: React Native autolinking hook missing from android/app/build.gradle"
            exit 1
          fi

          chmod +x android/gradlew
          echo 'Student Expo autolinking verified and repaired if necessary.'

      - name: Build Student APK
        working-directory: apps/student/android
        run: ./gradlew --no-daemon assembleDebug --stacktrace

      - name: Verify Student APK
        working-directory: apps/student
        run: |
          test -s android/app/build/outputs/apk/debug/app-debug.apk
          ls -lh android/app/build/outputs/apk/debug/app-debug.apk

      - name: Upload Student APK
        uses: actions/upload-artifact@v4
        with:
          name: studybro-student-debug-apk
          path: apps/student/android/app/build/outputs/apk/debug/*.apk
          if-no-files-found: error
