# StudyBro

Android build verification is configured through `.github/workflows/android-build.yml`.

The workflow runs on pushes to `main` and builds both the Student and Admin NSG Android debug APKs, then uploads them as workflow artifacts.
