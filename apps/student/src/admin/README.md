# NSG BRO Admin Control Foundation

M8 begins the phone-first Admin NSG control layer. The model supports role-based administration, content metadata, publish/hide/schedule states, ordering and targeted announcements.

The admin app should use the same backend as the student app. Authorization must be enforced server-side; client roles are UI hints, not security boundaries.