# NSG BRO Live Control Foundation

M9 establishes the remote-control contract for live announcements, feature flags, app policies and safe synchronization.

The student app must treat remote configuration as untrusted input and apply server-side authorization. Feature rollout percentages are policy data; actual audience assignment should be deterministic and enforced by the backend.