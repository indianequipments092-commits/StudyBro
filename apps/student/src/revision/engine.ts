import type {LearningRecommendation,RevisionItem,WeakArea} from "./types";
export const revisionIntervals=[1,3,7,14,30] as const;
export function nextRevisionDate(completedAt:Date,interval:number){const d=new Date(completedAt);d.setDate(d.getDate()+interval);return d.toISOString()}
export function buildRevisionQueue(items:RevisionItem[],now=new Date()){return items.filter(i=>!i.completed&&new Date(i.dueAt)<=now).sort((a,b)=>new Date(a.dueAt).getTime()-new Date(b.dueAt).getTime())}
export function buildWeakAreas(areas:WeakArea[]):WeakArea[]{return [...areas].sort((a,b)=>a.accuracy-b.accuracy||b.mistakes-a.mistakes)}
export function buildLearningRecommendations(weak:WeakArea[],revision:RevisionItem[],recentChapter?:{subject:"Physics"|"Chemistry"|"Biology";chapter:string}):LearningRecommendation[]{const out:LearningRecommendation[]=[];const top=buildWeakAreas(weak)[0];if(top)out.push({id:"weak-"+top.subject+"-"+top.chapter+"-"+top.topic,title:top.topic+" Revision",subject:top.subject,reason:"Your accuracy is "+top.accuracy+"% with "+top.mistakes+" mistakes.",priority:"high",action:"revision"});const due=buildRevisionQueue(revision)[0];if(due)out.push({id:"due-"+due.id,title:"Revision Due: "+due.topic,subject:due.subject,reason:"This topic is due for spaced revision.",priority:"high",action:"revision"});if(recentChapter)out.push({id:"continue-"+recentChapter.chapter,title:"Continue "+recentChapter.chapter,subject:recentChapter.subject,reason:"Resume your recent learning session.",priority:"medium",action:"continue"});return out.slice(0,3)}