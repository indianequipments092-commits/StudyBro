# NSG BRO Security Foundation

This milestone starts the client-side security boundary for device binding, session validation and privacy-aware dynamic watermark data.

Security is defense-in-depth: Android secure-window flags, encrypted offline storage, protected content URLs, server-side authorization and device policy belong in the platform/backend layers. Screenshot blocking is best-effort and device-dependent; it is not represented as a 100% guarantee.